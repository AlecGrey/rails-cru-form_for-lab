<h1><%= @song.name %></h1>

<h2>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) %></h2>

<h3>Genre: <%= link_to @song.genre.name, genre_path(@song.genre) %></h3>

<%= link_to "Edit Song", edit_song_path(@song) %>
<br>
<%= link_to "All Songs", songs_path %>